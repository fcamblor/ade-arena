# build
dist/
.astro/

# deps
node_modules/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# env
.env
.env.local
.env.*.local
.env.dev
.env.prod

# infra / secrets
# Per-contributor local secrets — created by `mise run bootstrap-local`.
infra/secrets/local.enc.yaml
infra/secrets/*.dec.yaml
infra/secrets/*.plain.yaml
# Per-contributor SOPS config — generated by `mise run bootstrap-local` with
# the contributor's own age public key. See .sops.yaml.example for the
# template.
.sops.yaml
supabase/.branches/
supabase/.temp/

# OS
.DS_Store

# editor
.vscode/
.idea/

# conductor
.context/
.playwright-mcp/

# claude-code local state
.claude/delta-gate.state.local.json
.claude/settings.local.json
